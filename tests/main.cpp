// doctest's main(). Per the docs, this file should be tiny and define exactly
// one macro before including the header so the framework's main() is emitted.
#define DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN
#include <doctest/doctest.h>
